import App from "../App";

const SearchBar = () => {
  return (
    <form>
      <input
        className="search-bar"
        type="text"
        placeholder="Search by name..."
      ></input>
    </form>
  );
};

export default SearchBar;
